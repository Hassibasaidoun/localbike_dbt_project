version: 2

models:
  - name: int_localbike_database__product
    description: '{{ doc("int_localbike_database__product") }}'
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
          - unique
          - not_null